<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@+id/progressBarBackground">
        <shape>
            <gradient
                android:angle="270"
                android:centerColor="@color/colorBackgroundProgresBarSpending"
                android:centerY="1.0"
                android:endColor="@color/colorBackgroundProgresBarSpending"
                android:startColor="@color/colorBackgroundProgresBarSpending" />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@+id/progressBarProgress">
        <clip>
            <shape>
                <gradient
                    android:angle="270"
                    android:centerColor="@color/colorBackgroundCardViewSpending"
                    android:centerY="1.0"
                    android:endColor="@color/colorBackgroundCardViewSpending"
                    android:startColor="@color/colorBackgroundCardViewSpending" />
            </shape>
        </clip>
    </item>
</layer-list>