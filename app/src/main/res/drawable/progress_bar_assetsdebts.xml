<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@+id/progressBarBackground">
        <shape>
            <gradient
                android:angle="270"
                android:centerColor="@color/colorBackgroundProgressBarAssetsDebts"
                android:centerY="1.0"
                android:endColor="@color/colorBackgroundProgressBarAssetsDebts"
                android:startColor="@color/colorBackgroundProgressBarAssetsDebts" />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@+id/progressBarProgress">
        <clip>
            <shape>
                <gradient
                    android:angle="270"
                    android:centerColor="@color/colorBackgroundCardViewAssetsDebts"
                    android:centerY="1.0"
                    android:endColor="@color/colorBackgroundCardViewAssetsDebts"
                    android:startColor="@color/colorBackgroundCardViewAssetsDebts" />
            </shape>
        </clip>
    </item>
</layer-list>